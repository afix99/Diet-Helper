# Leave both unset to run entirely on browser storage (no account, no server).
# Set both to switch on Supabase auth + cross-device sync.
#
# Find them in your Supabase project under Settings -> API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
